##### Other Sites Yaml

runtime: python27
api_version: 1
threadsafe: true

automatic_scaling: #Only applicable for appengine accounts with billing enabled
  max_instances: 1
  min_instances: 0

includes:
- includes-dev-prodview.yaml
- handlers.yaml
