##### Other Sites Yaml

runtime: python312


automatic_scaling: #Only applicable for appengine accounts with billing enabled
  max_instances: 1
  min_instances: 0

includes:
- includes-dev.yaml
- handlers.yaml
