{% from 'macros.j2' import docs_sdotermlink as sdotermlink with context %}
<!DOCTYPE html>
<html lang="en">
<!-- Generated from Home.j2 -->
    {% include 'docs/DocsHead.j2' with context %}
<body>
    {% include 'PageHeader.j2' with context %}
    <link rel="stylesheet" type="text/css" href="treeLinks/treeLinks.css" />
    <script src="treeLinks/treeLinks.js"></script>
    <script src="treeLinks/treeitemLinks.js"></script>
  <div id="mainContent">

      <h3>Full Hierarchy</h3>

      <p>
      Schema.org is defined as two hierarchies: one for textual property values, and one for the things that they describe. 
      </p> 

      <p>This is the main schema.org hierarchy: a collection of types (or "classes"), each of which has one or more parent types. Although a type may have more than one super-type, here we show each type in one branch of the tree only. There is also a parallel hierarchy for <a href="#datatype_tree">data types</a>.</p>

      <br/>
     <div style="display: block;text-align:left;">
      <strong>TREE VIEW TEST PAGES</strong>
      <ul>
        <li><a href="/docs/full.html">Non-collapsible Tree</a></li>
        <li><a href="/docs/full1.html">Collapsible Tree</a></li>
        <li><a href="/docs/full2.html">Collapsible Tree - with link symbols</a></li>
      </ul>
      <div style="color: red;"><strong>Collapsible Tree - with link symbols</strong></div>
      </div>

    {% for node in listings recursive %}
      {% if loop.depth == 1 %}
        <div>
            {% if node.title %}<h4 id="Listing{{loop.index}}">{{node.title}}</h4>{% endif %}
        <ul style="list-style: none;" role="tree" aria-labelledby="Listing{{loop.index}}"> 
      {% endif %}
      {% if not node.retired %}
        {% if node.subs %}
          <li role="treeitem"
            aria-level="{{loop.depth}}"
            aria-setsize="{{loop.length}}"
            aria-posinset="{{loop.index}}"
            {% if loop.depth == 1 %}aria-expanded="true"{% else %}aria-expanded="false"{% endif %}
          ><span class="treebranch">
            {{node.id}}
            </span>
            <small>{{ sdotermlink(node,arialabelpre="Link to ",text=" &#x1f517;")|safe }}</small>
            <ul style="list-style: none;" role="group">
                {{ loop(node.subs) }}
            </ul>
          
          </li>
        {% else %}
          <li  role="none"
            aria-level="{{loop.depth}}"
            aria-setsize="{{loop.length}}"
            aria-posinset="{{loop.index}}"
          > {% if node.termType == "Enumerationvalue" %}::{% endif %}
            {{node.id}}<small>{{ sdotermlink(node,arialabelpre="Link to ",text=" &#x1f517;")|safe }}</small>
          </li>
        {% endif %}

     {% endif %}
     {% if loop.depth == 1 %}
        </ul>
      {% endif %}
    {% endfor %}
            
              
      
</div><!-- mainContent -->


{% include 'PageFooter.j2' with context %}

</body>
</html>
      
      
